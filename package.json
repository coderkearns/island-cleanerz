{
  "name": "the-island-cleanerz",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist --nojekyll"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "html-minifier-terser": "^7.2.0",
    "vite": "^6.0.0"
  }
}
